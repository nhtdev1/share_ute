name: solution_repository
version: 1.0.0
publish_to: none
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^1.0.1
  cloud_firestore: ^1.0.1
  equatable: ^1.2.5
  file_picker_repository:
    path: ../file_picker_repository
  post_repository:
    path: ../post_repository